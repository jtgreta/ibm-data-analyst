# Environment template for the IBM Data Analyst capstone project
# Copy to `.env` and fill in the real values.
# `.env` is gitignored - never commit real credentials.

# Example custom variables:
# MY_API_KEY=your_api_key